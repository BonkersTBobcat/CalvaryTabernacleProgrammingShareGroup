# Lines that start with a # are called comments, and are not code.
# Anything typed on a line that starts with a # is ignored by the computer.
# They exist so you can describe what the code is doing in plain english,
# allowing other people to understand your code more easily.

# I will use the comments in this code to explain programming concepts in more detail.
#
# This is a function. Functions show up in the code as some word, like "print", followed by parenthesis, like "print()".
# Functions are bits of code, written by somebody, that can be run simply by using their name.
#
# The stuff inside the parenthesis is called the function parameters. Some functions take no parameters,
# and look like this: function()
#
# Other functions take 1 parameter, like the print function we are calling.
# The parameter in our case is just a bit of text. In programming text values are called string.
# You can remember this because a text is just a STRING of letters, numbers, and other stuff put together.
# In programming, strings are placed in quotes, otherwise the computer will think you are trying to type
# code and not a string, and it'll get confused.
#
# Then, finally, some functions take more than one parameter. Parameters are seperated by commas.
# For example: function(parameter1,parameter2)
print("Hello World")